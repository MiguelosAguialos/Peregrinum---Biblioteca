import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AtualizarreservaPage } from './atualizarreserva.page';

describe('AtualizarreservaPage', () => {
  let component: AtualizarreservaPage;
  let fixture: ComponentFixture<AtualizarreservaPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AtualizarreservaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
